# Dashboard Streamlit — SÉRÉNO
# Installer d’abord les dépendances PyPI, puis le paquet local « . » (plus fiable sur Streamlit Cloud).
# google-generativeai est volontairement exclu : il provoque un backtracking pip très long avec
# google-cloud-aiplatform et peut faire échouer le déploiement Cloud (timeout). Pour les tests
# « API Gemini » (page Connexions), en local : pip install -r requirements-gemini.txt
streamlit>=1.28
requests>=2.28
twilio>=9.0
fpdf2>=2.7.0
qrcode[pil]>=7.4
gspread>=6.0
google-auth>=2.0
google-cloud-storage>=2.14
google-cloud-aiplatform>=1.70,<2.0
.
